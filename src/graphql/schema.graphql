### This file was generated by Nexus Schema
### Do not make changes to this file directly


type Comment {
  author: User
  createdAt: DateTime
  id: String
  post: Post
  text: String
  updatedAt: DateTime
}

"""A custom scalar type representing a date and time."""
scalar DateTime

type Edge {
  cursor: String
  node: Post
}

type Me {
  email: String
  fullname: String
  iamge: String
  name: String
  userId: String
}

type Mutation {
  createPost(caption: String, imageUrl: String!): Post
  deletePost(id: String!): Post
  likePost(id: String!): Post
  login(email: String!, password: String!): Session!
  register(email: String!, fullname: String, name: String, password: String!): User!
  unlikePost(id: String!): Post
}

type PageInfo {
  endCursor: String
  hasNextPage: Boolean
}

type Post {
  author: User
  caption: String
  comments: [Comment]
  createdAt: DateTime
  id: String
  imageUrl: String
  likesCount: Int
  updatedAt: DateTime
}

type Query {
  getAllPosts: [Post]
  getPost(id: ID!): Post
  getUserById(id: ID!): User
  getUsers: [User]
  me: Me
  posts(after: String, first: Int): Response
}

type Response {
  edges: [Edge]
  pageInfo: PageInfo
}

type Session {
  accessToken: String
  email: String
  fullname: String
  iamge: String
  name: String
  userId: String
}

type User {
  createdAt: DateTime
  email: String
  followers: [User]
  following: [User]
  fullname: String
  iamge: String
  id: String
  name: String
  password: String
  posts: [Post]
  updatedAt: DateTime
}